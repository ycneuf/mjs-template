# ########################################################################### #
# Configuration très basique : il s'agit de servir un contenu statique !      #
#                                                                             # 
# Donc, pas de virtual host, de mod_rewrite, de gestion des accès, de SSL ou  #
# autre subitilité.                                                           #
# ########################################################################### #

# répertoire racine du site
server.document-root = "/htdocs"

# port d'écoute : le 80 car on est en http
server.port = 80

# utilisateur : www
# server.username = "www"
# server.groupname = "www"

# index
index-file.names = ( "index.html" )
